Screen1 As screen:
    OnVisible: =

    Rectangle1 As rectangle:
        Height: =768
        Width: =1366
        ZIndex: =1

    Button2 As button:
        DisplayMode: =DisplayMode.View
        Fill: =RGBA(255, 255, 255, 1)
        Height: =20
        RadiusBottomLeft: =30
        RadiusBottomRight: =30
        RadiusTopLeft: =30
        RadiusTopRight: =30
        Text: ="Button"
        Width: =757
        X: =287
        Y: =386
        ZIndex: =2

    Button2_1 As button:
        DisplayMode: =DisplayMode.View
        Fill: =RGBA(54, 176, 75, 1)
        Height: =Button2.Height
        RadiusBottomLeft: =30
        RadiusBottomRight: =30
        RadiusTopLeft: =30
        RadiusTopRight: =30
        Text: =""
        Width: =(Button2.Width/100)*Value(varper)
        X: =Button2.X
        Y: =Button2.Y
        ZIndex: =3

    Button1 As button:
        OnSelect: |
            =UpdateContext({LoadPopup: !LoadPopup});
            ClearCollect(
                Colpop,
                LargeData
            );
            ClearCollect(
                Colpop,
                LargeData
            );
            ClearCollect(
                Colpop,
                LargeData
            );
            ClearCollect(
                Colpop,
                LargeData
            );
            ClearCollect(
                Colpop,
                LargeData
            );
            ClearCollect(
                Colpop,
                LargeData
            );
            ClearCollect(
                Colpop,
                LargeData
            );
            ClearCollect(
                Colpop,
                LargeData
            );
            ClearCollect(
                Colpop,
                LargeData
            );
            ClearCollect(
                Colpop,
                LargeData
            );
            ClearCollect(
                Colpop,
                LargeData
            );
            ClearCollect(
                Colpop,
                LargeData
            );ClearCollect(Colpop,LargeData);
            ClearCollect(
                Colpop,
                LargeData
            );
            ClearCollect(
                Colpop,
                LargeData
            );
            ClearCollect(
                Colpop,
                LargeData
            );
            ClearCollect(
                Colpop,
                LargeData
            );
            ClearCollect(
                Colpop,
                LargeData
            );
            ClearCollect(
                Colpop,
                LargeData
            );
            ClearCollect(
                Colpop,
                LargeData
            );
            ClearCollect(
                Colpop,
                LargeData
            );
            ClearCollect(
                Colpop,
                LargeData
            );
            UpdateContext({varper: 10});
            ClearCollect(
                Colpop,
                LargeData
            );
            ClearCollect(
                Colpop,
                LargeData
            );
            ClearCollect(
                Colpop,
                LargeData
            );
            ClearCollect(
                Colpop,
                LargeData
            );
            ClearCollect(
                Colpop,
                LargeData
            );
            ClearCollect(
                Colpop,
                LargeData
            );
            ClearCollect(
                Colpop,
                LargeData
            );
            UpdateContext({varper: 50});ClearCollect(Colpop,LargeData);ClearCollect(Colpop,LargeData);ClearCollect(Colpop,LargeData);ClearCollect(Colpop,LargeData);
            ClearCollect(
                Colpop,
                LargeData
            );
            ClearCollect(
                Colpop,
                LargeData
            );
            ClearCollect(
                Colpop,
                LargeData
            );
            ClearCollect(
                Colpop,
                LargeData
            );
            ClearCollect(
                Colpop,
                LargeData
            );
            ClearCollect(
                Colpop,
                LargeData
            );
            ClearCollect(
                Colpop,
                LargeData
            );
            ClearCollect(
                Colpop,
                LargeData
            );
            ClearCollect(
                Colpop,
                LargeData
            );
            ClearCollect(
                Colpop,
                LargeData
            );
            ClearCollect(
                Colpop,
                LargeData
            );
            ClearCollect(
                Colpop,
                LargeData
            );
            ClearCollect(
                Colpop,
                LargeData
            );
            ClearCollect(
                Colpop,
                LargeData
            );
            ClearCollect(
                Colpop,
                LargeData
            );
            ClearCollect(
                Colpop,
                LargeData
            );
            ClearCollect(
                Colpop,
                LargeData
            );
            UpdateContext({varper: 100});
        Text: ="Button"
        X: =566
        Y: =528
        ZIndex: =4

    Label1 As label:
        Align: =Align.Center
        Color: =RGBA(255, 255, 255, 1)
        Fill: =Rectangle1.Fill
        Text: =varper&"%"
        X: =536
        Y: =258
        ZIndex: =5

